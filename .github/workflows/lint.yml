name: lint

on:
  push:
    branches: [main, dev]
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: find . -name "*.sh" -not -path "./.git/*" -print0 | xargs -0 shellcheck --shell=bash --severity=error
